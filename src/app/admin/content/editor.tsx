"use client";
import { useState } from "react";
const fields=[["heroTitle","Hero title"],["heroText","Hero description"],["heroPrimary","Primary button"],["heroSecondary","Secondary button"],["trustTitle","Why us heading"],["ctaTitle","Trial CTA heading"],["ctaText","Trial CTA text"],["footerText","Footer text"]] as const;
export default function ContentEditor({initial}:{initial:Record<string,string>}){const [values,setValues]=useState(initial);const [status,setStatus]=useState("");async function save(){setStatus("Saving...");const res=await fetch("/api/admin/content",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)});const data=await res.json();setStatus(data.ok?"Saved successfully.":data.error||"Unable to save.")}return <div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2">{fields.map(([key,label])=><label key={key} className={key==="heroText"||key==="ctaText"||key==="footerText"?"md:col-span-2":""}><span className="mb-2 block text-sm font-semibold">{label}</span>{key.includes("Text")||key==="footerText"?<textarea rows={4} value={values[key]||""} onChange={e=>setValues({...values,[key]:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#0e5a45]"/>:<input value={values[key]||""} onChange={e=>setValues({...values,[key]:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-[#0e5a45]"/>}</label>)}</div><div className="mt-6 flex items-center gap-4"><button onClick={save} className="rounded-xl bg-[#0e5a45] px-5 py-3 text-sm font-semibold text-white">Save website content</button><span className="text-sm text-slate-500">{status}</span></div></div>}
